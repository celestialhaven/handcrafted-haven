"use client";
import { FormEvent,useState } from "react";import styles from "../dashboard-pages.module.css";
type Settings={email:string;orderEmails:boolean;reviewEmails:boolean;marketingEmails:boolean};
export default function SettingsForm({settings}:{settings:Settings}){const[status,setStatus]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Saving…");const data=new FormData(e.currentTarget);const body={email:data.get("email"),orderEmails:data.has("orderEmails"),reviewEmails:data.has("reviewEmails"),marketingEmails:data.has("marketingEmails")};const response=await fetch("/api/dashboard/settings",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const result=await response.json();setStatus(response.ok?result.message:result.error||"Unable to save settings.");}return <form className={styles.form} onSubmit={submit}><div className={styles.field}><label htmlFor="email">Account email</label><input id="email" name="email" type="email" defaultValue={settings.email} required/></div><fieldset className={styles.checks}><legend>Email notifications</legend><label><input type="checkbox" name="orderEmails" defaultChecked={settings.orderEmails}/><span><strong>Order updates</strong><br/>Receive email when a customer places or updates an order.</span></label><label><input type="checkbox" name="reviewEmails" defaultChecked={settings.reviewEmails}/><span><strong>New reviews</strong><br/>Receive email when a customer reviews one of your products.</span></label><label><input type="checkbox" name="marketingEmails" defaultChecked={settings.marketingEmails}/><span><strong>Marketplace news</strong><br/>Receive product tips and Handcrafted Haven announcements.</span></label></fieldset><button>Save Settings</button><p className={styles.status} role="status">{status}</p></form>}
