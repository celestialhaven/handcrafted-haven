"use client";
import { FormEvent, useState } from "react";
import styles from "../dashboard-pages.module.css";
export default function ProfileForm({profile}:{profile:{displayName:string;location:string;bio:string;specialties:string[]}}){const[status,setStatus]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Saving…");const data=new FormData(e.currentTarget);const response=await fetch("/api/dashboard/profile",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(data))});const result=await response.json();setStatus(response.ok?result.message:result.error||"Unable to update profile.");}return <form className={styles.form} onSubmit={submit}><div className={styles.formGrid}><div className={styles.field}><label htmlFor="displayName">Shop display name</label><input id="displayName" name="displayName" defaultValue={profile.displayName} required/></div><div className={styles.field}><label htmlFor="location">Location</label><input id="location" name="location" defaultValue={profile.location}/></div></div><div className={styles.field}><label htmlFor="specialties">Specialties</label><input id="specialties" name="specialties" defaultValue={profile.specialties.join(", ")} placeholder="Pottery, Home Decor"/></div><div className={styles.field}><label htmlFor="bio">Biography</label><textarea id="bio" name="bio" defaultValue={profile.bio}/></div><button>Save Profile</button><p className={styles.status} role="status">{status}</p></form>}
